{
  "$id": "https://github.com/daniloaguiarbr/sqlite-graphrag/schemas/enrich-summary.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Final summary NDJSON line emitted by `sqlite-graphrag enrich`. Always the last line. The 'summary' sentinel field distinguishes it from per-item events. backend_invoked added in v1.0.84 by ADR-0042.",
  "properties": {
    "completed": {
      "description": "Items successfully processed.",
      "minimum": 0,
      "type": "integer"
    },
    "cost_usd": {
      "description": "Cumulative LLM cost in USD. Zero for dry-run and OAuth runs.",
      "minimum": 0,
      "type": "number"
    },
    "elapsed_ms": {
      "description": "Total wall-clock time in milliseconds.",
      "minimum": 0,
      "type": "integer"
    },
    "failed": {
      "description": "Items that failed processing.",
      "minimum": 0,
      "type": "integer"
    },
    "items_total": {
      "description": "Total items discovered by the scan phase.",
      "minimum": 0,
      "type": "integer"
    },
    "llm_parallelism": {
      "type": [
        "string",
        "number",
        "boolean",
        "null"
      ]
    },
    "operation": {
      "description": "Rust Debug format of the EnrichOperation variant (e.g. 'MemoryBindings', 'EntityDescriptions', 'BodyEnrich').",
      "type": "string"
    },
    "skipped": {
      "description": "Items skipped (filtered out or not-yet-implemented operation).",
      "minimum": 0,
      "type": "integer"
    },
    "summary": {
      "const": true,
      "description": "Sentinel field; always true. Distinguishes this line from per-item events."
    },
    "backend_invoked": {
      "description": "v1.0.84 (ADR-0042): backend LLM that ran the live embedding across this enrich run (claude, codex, or none). Omitted when enrich ran without embedding.",
      "type": "string",
      "enum": ["claude", "codex", "opencode", "none", "auto"]
    }
  },
  "required": [
    "summary",
    "operation",
    "items_total",
    "completed",
    "failed",
    "skipped",
    "cost_usd",
    "elapsed_ms"
  ],
  "title": "EnrichSummary",
  "type": "object"
}
